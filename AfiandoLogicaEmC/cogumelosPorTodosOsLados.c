#include <stdio.h>

int main() {
  int minutos, cogumelosColhidos;

  // Lê a quantidade de minutos disponíveis para colheita
  scanf("%d", &minutos);

  // TODO: Calcule e imprima a quantidade de cogumelos colhidos.
  
  printf("%d", (int) (cogumelosColhidos = minutos/3));
  return 0;
}